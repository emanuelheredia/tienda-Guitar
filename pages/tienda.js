import React from "react";
import Layout from "../components/Layout";

const Tienda = () => {
	return (
		<div>
			<Layout pagina="Tienda virtual">Desde tienda</Layout>
		</div>
	);
};

export default Tienda;
